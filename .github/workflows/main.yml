name: Main

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to run tests against'
        type: environment
        required: true

jobs:
 main:
    name: Main
    runs-on: ubuntu-latest
    env:
      TARGET_ENV: ${{ github.event.inputs.environment }}
    steps:
      - name: Access env token
        run: env
